import styled from "styled-components";

export const ContainerMain = styled.aside`
  padding: 1.4rem 1.1rem;
  background: #000;
`;

export const ContainerItem = styled.aside`
  display: flex;
  padding: 0.2rem 0;

  svg {
    width: 13px;
    margin-right: 0.5rem;
    color: #fff;
  }
`;